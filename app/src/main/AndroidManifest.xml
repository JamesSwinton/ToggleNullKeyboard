<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.zebra.jamesswinton.togglenullkeyboard">

  <!-- EMDK Permission -->
  <queries>
    <package android:name="com.symbol.emdk.emdkservice" />
  </queries>

  <!-- EMDK Permission -->
  <uses-permission android:name="com.symbol.emdk.permission.EMDK" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.ToggleDefaultLauncher">

    <!-- EMDK Lib -->
    <uses-library android:name="com.symbol.emdk" android:required="true"/>

    <activity
      android:name="com.zebra.jamesswinton.togglenullkeyboard.MainActivity"
      android:theme="@style/Theme.Transparent">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

  </application>

</manifest>